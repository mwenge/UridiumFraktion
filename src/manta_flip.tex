\chapter{MANTA/FLIP} 
\label{sec:ships}
\lstset{style=6502Style}

Perhaps one of the lovelist accomplishments in \textit{Uridium} is the animation the game
performs when the player's manta changes horizontal direction. There is a suprising amount
of depth to the way this maneouvre is rendered and it contains a number of subtle details
that evidence the care and attention that was paid to its implementation. Together with
the silky scrolling I believe the endeavour for detailed realism in this animation
is responsible for the immersion this game creates, even for the modern player. 

The first remarkable thing about the manouevre is the sheer number of sprites that are
devoted to it. In the impressionistic rendering below you get a sense of how dense
the animation is, how many individual frames have been devoted to its creation.

\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/manta_flip.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

If we unpick this impressionistic rendering and show each sprite in isolation we get
a better sense of just how many are involved, 16 in total.
\begin{figure}[H]
    \hspace{0.2cm}
    \begin{adjustbox}{width=13.5cm,center}
      \includegraphics[width=15cm]{src/manta_flip/manta_flip_wide.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

As we look closer we can see the manouevre is broken up into two parts: a flip, and a roll.
It is no coincidence that these two elements to the manouvre are symmetrical in length, this
symmetry is part of what underlines its visual appeal. The flip occurs over eight individual
frames.
\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/manta_flip_1.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

The roll is the same, the manta rotates a full 180 degrees, completing the turn.
\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/manta_flip_2.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

Notice how we could break each of these halves of the manouevre equally again. Each reaches
its halfway point in the very middle. 

Of course, as we must expect by now, a sequence like this must be governed by a data structure.
This is provided by \icode{mantaFlipFromLeftToRight} which we give below. The main body of this
structure is, as we would expect, the sequence of sprite values that make up the frames of the
animation.

\begin{lstlisting}[basicstyle=\footnotesize\ttfamily]
mantaFlipFromLeftToRight
  .BYTE 16                                  ; No. of Frames in Animation
  .BYTE $41,$40,$4F,$4E,$4D,$4C,$4B,$4A     ; Sprites for the roll..
  .BYTE $49,$67,$68,$69,$6A,$6B,$6C,$6D,$59 ; .. and the flip.
  .BYTE $28                                 ; Whether to adjust shadow.
\end{lstlisting}

If we compare the data structure used to define the animation with the order of the frames
below we can see that they are utilized in reverse order. We start at the end with sprite \icode{\$59}
and end on sprite \icode{\$41}.

\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/6_mantaFlipFromLeftToRight_0.png}%
    \end{adjustbox}
    \vspace{0.2cm}
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/6_mantaFlipFromLeftToRight_8.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

But there is more to the data structure than just this. Our comment on the last byte mentions something
do with a shadow adjustment. To see what this might be we need to take a look at how the animation is
rendered over the surface of a dreadnought.

\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/flip_diagram.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

Perhaps it is not immediately obvious? Pay attention to the behaviour of the shadows below the manta.
It might be easier to notice the effect if we zoom into the \icode{flip}.

\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/flip_diagram_1.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

If it is still not obvious, that might a testament to the realism of the effect. You are seeing what
you expect to see, so nothing stands out. The hidden hand of extreme attention to detail has concealed
its workings from you. Let's see if you can see something wrong if we remove the effect:

\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/flip_diagram_1_no_offset.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

Hopefully it is evident now: as the player's ship flips back the shadow on the surface of the dreadnought
moves away from the manta. This is not happening by accident, but because it had to be implemented specifically
to create the impression that the manta is lifting up from the surface as it turns back in on itself. 

As the manta rolls, it returns towards the surface, eventually resuming its original altitude. As it does so,
its shadow gets closer.

\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=13cm,center}
      \includegraphics[width=15cm]{src/manta_flip/flip_diagram_2.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

Underlying this effect is an array of offsets, each one paired with a frame in the animation. And like the
animation in \icode{mantaFlipFromLeftToRight}, this array is designed to be read from the end. This means
we can use the same index we use to read in sprite frames from \icode{mantaFlipFromLeftToRight} to read in the 
the appropriate offset to add to the position of the sprite's shadow.

The array is given below. Interesting to note is the way the movement of the shadow is designed to speed up and
then slow down before arriving at its peak altitude, and likewise when descending back to the surface.

\begin{lstlisting}
mantaShadowOffsets   
  .CHAR  0,-1,-2,-3,-3,-2,-2, -1 ; Shadow moves towards the manta (Roll).
  .CHAR  0, 1, 2, 3, 3, 2, 2,  1 ; Shadow moves away from the manta (Flip).
\end{lstlisting}

This naturalistic movement is emblematic of the kind of deep consideration this manoeuvre has been given. Not only
has serious thought been given to conveying the vertical movement of the player's ship, but the game has found a way
of expressing the acceleration and deceleration that would be inherent to a flip and roll of this sort.

On the opposite page we can seehow all of this, the \icode{mantaFlipFromLeftToRight} animation data structure, 
plus the \icode{mantaShadowOffsets} array, come together in a short routine that combines them both to fetch a
new frame of the animation and update the position and appearance of the  manta and shadow sprites as appropriate.

Note how there are two things determining whether we should draw a new frame of the animation. The first is
\icode{mantaState} which, if an animation is active, will have its top bit set (e.g. \icode{\$80}, making it 
a negative value). The second is the \icode{frameRate}: since this routine may be visited dozens of times a
second it will only ever need to do anything new once the current frame of the animation has been displayed
for a long enough period.

But our quest for naturalism has created a complication that must be resolved. During its maneouvres the 
dimension and orientation of the manta changes. When flipping backwards, there is a short period in which
the nose is facing fully upwards making the ship narrower along the horizontal plane (e.g.
\raisebox{-0.2\totalheight}{\includegraphics[height=0.4cm]{src/manta_sprites/6_69.png}}, 
\raisebox{-0.2\totalheight}{\includegraphics[height=0.4cm]{src/manta_sprites/6_6A.png}}, 
\raisebox{-0.2\totalheight}{\includegraphics[height=0.4cm]{src/manta_sprites/6_6B.png}} 
).
Likewise, when the
ship is rolling there is a short period when it is narrower along the vertical plane (e.g.
\raisebox{-0.2\totalheight}{\includegraphics[height=0.4cm]{src/manta_sprites/6_4E.png}}, 
\raisebox{-0.2\totalheight}{\includegraphics[height=0.4cm]{src/manta_sprites/6_4D.png}}, 
\raisebox{-0.2\totalheight}{\includegraphics[height=0.4cm]{src/manta_sprites/6_4C.png}} 
). When detecting whether the ship has collided with any of the infrastructure on the dreadnought's
surface we will need to take account of this somehow. The solution is to create an array
of flags, with one for each manta sprite, that tells us the approximate bounding box of the player's
ship. In this array, the left-hand nibble will be \icode{\$8} if we should include the top and bottom
of the normal manta sprite (
\raisebox{-0.2\totalheight}{\includegraphics[height=0.4cm]{src/manta_sprites/6_40.png}} 
) in the bounding box. The right-hand nibble meanwhile will be \icode{\$0} if we should include the left
and right of the normal manta sprite, and \icode{\$1} otherwise.

\begin{lstlisting}[escapechar=\%]
mantaDimensionHintsArray
  ;     %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_40.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_41.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_42.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_43.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_44.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_45.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_46.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_47.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_48.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_49.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_4A.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_4B.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_4C.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_4D.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_4E.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_4F.png}}%
  .BYTE $80,$80,$80,$80,$00,$00,$00, $80,$80,$80,$80,$80,$00,$00,$00,$80
  ;     %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_50.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_51.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_52.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_53.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_54.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_55.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_56.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_57.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_58.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_59.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_5A.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_5B.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_5C.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_5D.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_5E.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_5F.png}}%
  .BYTE $80,$80,$80,$80,$00,$00,$00, $80,$80,$80,$80,$80,$00,$00,$00,$80
  ;     %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_60.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_61.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_62.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_63.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_64.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_65.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_66.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_67.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_68.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_69.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_6A.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_6B.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_6C.png}}%  %\raisebox{-0.2\totalheight}{\includegraphics[height=0.32cm]{src/manta_sprites/6_6D.png}}% 
  .BYTE $80,$80,$81,$81,$81,$80,$80, $80,$80,$81,$81,$81,$80,$80
\end{lstlisting}


\clearpage
\begin{lstlisting}[basicstyle=\scriptsize\ttfamily]
;-----------------------------------------------------------------------------
; Draw a frame from a manouevre animation (in our example, mantaFlipFromLeftToRight)..
; This routine called by the main game loop dozens of times a second!
;-----------------------------------------------------------------------------
DrawTheAnimation   
  LDA mantaState                       ; Check if we should draw a new frame.
  BMI GetNextFrameOfAnimation          ; If so, then get the next frame..
  JMP DrawMantaAnimationFrame          ; Otherwise just redraw the current one.

GetNextFrameOfAnimation
  ; Check our frame rate before displaying a new one.
  LDA frameRate                        ; Check if we should display new frame. 
  AND #$01                             ; If it's an odd number, get a new frame.
  BNE DrawMantaAnimationFrame          ; Otherwise draw the current one again.

  ; Get the new sprite. mantaAnimationLoPtr is a pointer into the
  ; mantaFlipFromLeftToRight animation data structure (in this example).
  LDY currentFrame                     ; Get the current frame index.
  LDA (mantaAnimationLoPtr),Y          ; Get next sprite from the data structure.
  STA newSpriteValue                   ; Store it as the new sprite frame.

  ; Get the shadow offset for this frame of the animation.
  ; We will only do this if the appropriate byte is non-zero in the animation's
  ; data structure.
  LDA shadowDepthDuringMantaAnimation  ; Check whether to display a shadow.
  BEQ DecrementFrameIndex              ; If not, skip adding the shadow offset.
  LDA mantaShadowOffsets,Y             ; Get shadow offset for new frame.
  ADC mantaShadowOffset                ; Add the current offset to it.
  STA mantaShadowOffset                ; Store as the new shadow offset.
DecrementFrameIndex
  DEC currentFrame                     ; Decrement frame index for next time around.

  ; Draw the animation frame.
DrawMantaAnimationFrame   
  LDA #$06                             ; Get sprite index for the manta.
  STA spriteIndex                      ; Store it in spriteIndex.
  JSR GetCurrentSprite                 ; Load current sprite info for the manta.
  LDA newSpriteValue                   ; Get our new frame and..
  STA currentSpriteValue               ; .. update the manta sprite with it.
  ADC #$30                             ; Add $30 to point A to the shadow sprite.
  STA mantaShadowSpriteValue           ; Store the new frame in it too.
  LDA mantaCurrentYPos                 ; Get manta's current Y position.
  STA currentSpriteYPos                ; Store it as the manta sprite's position.
  JSR DisplayCurrentSprite             ; Display the manta sprite with the new values.

  ; Draw the Manta's shadow.
  INC spriteIndex                      ; Move sprite index to point to the shadow.
  JSR GetCurrentSprite                 ; Load current sprite info for the shadow.
  LDA mantaShadowSpriteValue           ; Get the new sprite for the shadow.
  STA currentSpriteValue               ; .. and update the shadow sprite with it.

  ; Calculate the vertical position of the shadow.
  LDA mantaShadowOffset                ; Get the offset for the shadow.
  LSR                                  ; Divide it by 2.
  ADC mantaCurrentYPos                 ; Add the manta's Y position to it.
  STA currentSpriteYPos                ; Store it as the shadow's Y position.

  ; Calculate the horizontal position of the shadow.
  LDA #MANTA_HORIZONTAL_POSITION       ; Get the manta's fixed X position.
  ADC mantaShadowOffset                ; Add the shadow offset to it.
  STA currentSpriteXPos                ; Store it as the shadow's X position.

  JSR DisplayCurrentSprite             ; Display the shadow sprite with the new values.
\end{lstlisting}

We get a better sense of how this works when we look at the dimension hints used in practice. Below
we can see that the full bounding box of the manta should be checked for the first three frames (\icode{\$80}), but
that the top and bottom of the bounding box should be ignore for the last two (\icode{\$00}) because the manta has
become narrow on the horizontal axis.
\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=10cm,center}
      \includegraphics[width=15cm]{src/manta_flip/dimension_hints/6_bankLeftTo90DegreesFacingRight_dimension_hints.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}
Likewise in the example below, we check the full bounding box for the first three \icode{\$80} but ignore the left and right of the
bounding box (\icode{\$81}) when the manta becomes narrow on the vertical axis.
\begin{figure}[H]
    \centering
    \begin{adjustbox}{width=10cm,center}
      \includegraphics[width=15cm]{src/manta_flip/dimension_hints/6_flip_dimension_hints.png}%
    \end{adjustbox}
\end{figure}
\vspace{-0.5cm}

The full collision detection routine is given opposite and you can see how extensive it has to be - it is almost as verbose
as the animation routine on the previous page. It utilizes the \icode{mantaDimensionHintsArray} to decide which of the left,
right, top, and bottom sides of the manta to check for surface furniture that will destroy the player's ship if they collide
with it. One of its few economies lies in the way it checks for destructive objects: any that are defined with a character code
less than \icode{\$90} are by definition lethal to the manta. 

This was a design decision we observed in our discussion of
\hyperref[sec:bangbang]{\textcolor{blue}{the player's bullets}} in an earlier chapter,
namely how the characters used to define the surface were deliberately chosen according to their purpose so that a simple
range check such as the one here could be used to detect whether they were harmful to the player or resistant to the ministrations
of the player's bullets.

\clearpage
\begin{lstlisting}[basicstyle=\scriptsize\ttfamily]
CheckForMantaCollisionsWithSurface
  LDY newSpriteValue              ; Get the sprite frame.
  LDA mantaDimensionHintsArray,Y  ; Get its dimension hint from the array.
  STA mantaDimensionHint          ; Store it in mantaDimensionHint.
  AND #$01                        ; Extract the low bit (horizontal dimension).
  TAY                             ; Store it in Y
  BNE CheckCentreOfManta          ; If low bit is a 1, only check the centre.

CheckLeftSideOfManta
  LDA (centreOfMantaLoPtr),Y      ; Get the character under the manta's centre.
  BPL CheckRightSideOfManta       ; If it's an inoffensive character, check the right.
  CMP #$90                        ; Check if it's a destructive character.
  BCS CheckRightSideOfManta       ; If it's not, check the right hand side.
  LDA #$80                        ; If it is, set the 'we've been hit' flag..
  STA shipHasBeenHit              ; in shipHasBeenHit.

CheckRightSideOfManta
  LDY #$02                        ; Get the character under the..
  LDA (centreOfMantaLoPtr),Y      ; .. right hand side of the manta.
  BPL CheckCentreOfManta          ; If it's an inoffensive character, check the centre.
  CMP #$90                        ; Check if it's a destructive character.
  BCS CheckCentreOfManta          ; If it's not, check the right hand side.
  LDA #$80                        ; If it is, set the 'we've been hit' flag..
  STA shipHasBeenHit              ; in shipHasBeenHit.

CheckCentreOfManta
  LDY #$01                        ; Get the character under the..
  LDA (centreOfMantaLoPtr),Y      ; .. centre of the manta.
  BPL CheckTopSideOfManta         ; If it's an inoffensive character, check the top.
  CMP #$90                        ; Check if it's a destructive character.
  BCS CheckTopSideOfManta         ; If it's not, check the top side.
  LDA #$80                        ; If it is, set the 'we've been hit' flag..
  STA shipHasBeenHit              ; in shipHasBeenHit.

CheckTopSideOfManta
  LDA mantaDimensionHint          ; Get our dimension hint.
  BPL ReturnFromCollisionCheck    ; If top bit is 0, don't check top and bottom.
  DEC centreOfMantaHiPtr          ; Move our char pointer to..
  DEC centreOfMantaHiPtr          ; .. the top of the manta.
  LDA (centreOfMantaLoPtr),Y      ; Get the character there.
  BPL CheckBottomSideOfManta      ; If it's an inoffensive character, check the bottom.
  CMP #$90                        ; Check if it's a destructive character.
  BCS CheckBottomSideOfManta      ; If it's not, check the bottom side.
  LDA #$80                        ; If it is, set the 'we've been hit' flag..
  STA shipHasBeenHit              ; in shipHasBeenHit.

CheckBottomSideOfManta
  LDA centreOfMantaHiPtr          ; Increment the Hi Ptr by 4 to move our character..
  ADC #$04                        ; .. pointer down to the bottom of the manta..
  STA centreOfMantaHiPtr          ; .. again.
  LDA (centreOfMantaLoPtr),Y      ; Get the character there.
  BPL RestorePointerToCentre      ; If it's an inoffensive character, check the bottom.
  CMP #$90                        ; Check if it's a destructive character.
  BCS RestorePointerToCentre      ; If it's not, check the bottom side.
  LDA #$80                        ; If it is, set the 'we've been hit' flag..
  STA shipHasBeenHit              ; in shipHasBeenHit.

RestorePointerToCentre
  DEC centreOfMantaHiPtr          ; Move our Hi Ptr back to the ..
  DEC centreOfMantaHiPtr          ; .. centre of the manta.
ReturnFromCollisionCheck
  RTS
\end{lstlisting}
